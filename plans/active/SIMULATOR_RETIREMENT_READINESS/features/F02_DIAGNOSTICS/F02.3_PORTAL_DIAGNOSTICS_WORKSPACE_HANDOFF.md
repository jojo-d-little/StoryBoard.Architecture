# F02.3: Portal Diagnostics Workspace Pass Handoff

Status: Complete
Feature packet: ../F02_FEATURE.md
Area profile: ../../../area-profiles/04_WEBPORTAL_PROFILE.md
Owner session: WebPortal diagnostics workspace implementation and manual acceptance

## Purpose

1. Implement the first-class Portal Diagnostics workspace and direct trace workflow using the approved Portal event model.

## Preconditions

1. F02.2 source inventory and event model are complete.
2. Read the locked Diagnostics workflow direction in the main readiness plan.
3. Preserve capability gating so development diagnostics remain unavailable in production mode.

## Authorized Scope

1. Primary area: `StoryBoard.WebPortal/Storyboard.WebPortal/**` and focused Portal tests.
2. Inherit `04_WEBPORTAL_PROFILE.md`.
3. Do not implement backend log collection, shared log storage, or layout-slot configuration as a prerequisite for diagnostics.

## Required Output

1. A permanent first-level Diagnostics workspace reachable from Dev Tools.
2. Direct `Start Trace` / `Stop Trace`, `Show Console` / `Hide Console`, `Clear`, profile/scope, filter, and capture-status controls.
3. Independent capture, visibility, docking, filtering, and clear-state behavior; hiding the console must not stop capture, and stopping must not clear entries.
4. Visible entry count, active profile, capture boundary, and truncation state.

## Validation

1. Focused component/workflow tests for the direct interaction sequence and independent state transitions.
2. Capability-gated development and production-negative tests.
3. Portal build and applicable visual/browser validation.

## Execution Record

### Scope Completed

1. Implemented the first-level Diagnostics workspace with direct trace start/stop, console visibility, clear, profile/scope, display filters, capture status, retention/truncation state, and export entry points.
2. Routed developer tooling through the standard mode/configuration and slot/skeleton definitions; development tooling occupies the reserved lower grid rows while normal mode leaves those rows unpopulated.
3. Kept capture, visibility, filtering, clearing, and export behavior independent so hiding the console does not stop capture and stopping does not clear retained entries.

### Files Changed

1. `Storyboard.WebPortal/src/App.tsx`, `src/components/DiagnosticsWorkspace.tsx`, `src/diagnostics/diagnosticsScope.ts`, and related diagnostics styles/configuration.
2. Standard orchestration composition, slot, and skeleton configuration for the development-tool rows.

### Contract/Interface Impact

1. No shared contract change. Workspace availability remains mode/configuration driven.

### Validation Results

1. Portal test suite passes: 27 files, 217 tests.
2. Production TypeScript/Vite build succeeds.
3. Manual Dev Simulator validation confirmed trace controls, diagnostics display, mode-specific visibility, and usable developer tooling placement.

### Remaining Gaps

1. External-window docking remains a future UX option; the accepted current baseline is the development-tool rows in the standard layout.

### Next Pass

1. F02.4 Portal trace export — complete.

# F02.2: Portal Trace Inventory And Event Model Pass Handoff

Status: Complete
Feature packet: ../F02_FEATURE.md
Area profile: ../../../area-profiles/04_WEBPORTAL_PROFILE.md
Owner session: WebPortal diagnostics implementation and validation

## Purpose

1. Inventory the Portal-side diagnostic sources and define a readable, structured event model that preserves the metadata needed for future manual or automated three-log alignment.

## Preconditions

1. F02.1 correlation convention is approved or explicitly recorded as no-contract-change.
2. Read the existing Portal diagnostics console, host API client, session polling, command, renderer, audio, cache, authentication, discovery, and orchestration workflows.

## Authorized Scope

1. Primary area: `StoryBoard.WebPortal/Storyboard.WebPortal/**` and focused Portal tests.
2. Inherit `04_WEBPORTAL_PROFILE.md`.
3. Do not add browser filesystem access, backend-log aggregation, or new Host/GameEngine endpoints.

## Required Output

1. A source/category inventory covering authentication, discovery, session lifecycle, transport/request activity, commands, polling/deltas, renderer, presentation/effects, audio, assets/cache, orchestration, and failures.
2. A Portal-local structured event shape with sequence, UTC timestamp, source/category, severity, event/phase, message, optional duration, correlation metadata, and structured details.
3. Explicit rules for sensitive-data exclusion, bounded retention, and missing optional fields.

## Validation

1. Focused Portal tests for event normalization, source/category mapping, metadata preservation, and bounded retention behavior.
2. Portal build and existing test suite.

## Execution Record

### Scope Completed

1. Inventoried Portal diagnostic sources across authentication, discovery, session lifecycle, transport, commands, polling/deltas, renderer, presentation, audio, assets/cache, orchestration, and failures.
2. Implemented a Portal-local normalized trace event shape with sequence, UTC timestamp, source/category, severity, message, correlation metadata, and structured details.
3. Preserved optional correlation and scope fields without requiring a new shared transport contract; sensitive detail keys are redacted during normalization.
4. Added bounded in-memory retention and source/category mapping tests as the foundation for later workspace and export behavior.

### Files Changed

1. `Storyboard.WebPortal/src/diagnostics/portalTrace.ts` and focused normalization/export tests.
2. Portal workflow diagnostic call sites and source/category mapping used by the diagnostics workspace.

### Contract/Interface Impact

1. No shared contract change. The approved F02.1 correlation fields are preserved where Portal already has them.

### Validation Results

1. Portal test suite passes: 27 files, 217 tests.
2. Production TypeScript/Vite build succeeds.
3. Manual trace review confirmed readable structured events and preserved correlation/session metadata.

### Remaining Gaps

1. Event volume and semantic coverage will continue to be tuned against real diagnosis scenarios; this is a follow-up refinement, not an inventory blocker.

### Next Pass

1. F02.3 Portal Diagnostics workspace — complete.

# F02.4: Portal Trace Export Pass Handoff

Status: Complete
Feature packet: ../F02_FEATURE.md
Area profile: ../../../area-profiles/04_WEBPORTAL_PROFILE.md
Owner session: WebPortal trace export implementation and manual acceptance

## Purpose

1. Export the complete bounded Portal trace in readable text and structured NDJSON without turning the Portal into a backend-log aggregation client.

## Preconditions

1. F02.2 event model is complete.
2. F02.3 Diagnostics workspace and capture boundary are complete.

## Authorized Scope

1. Primary area: `StoryBoard.WebPortal/Storyboard.WebPortal/**` and focused Portal tests.
2. Inherit `04_WEBPORTAL_PROFILE.md`.
3. Do not implement the future three-log merge utility or collect GameHost/GameEngine files.

## Required Output

1. Default full-buffer readable-text export.
2. Structured NDJSON export preserving one event per line and the approved metadata fields.
3. Explicitly labeled filtered/visible export if offered; filtered output must not be confused with the complete capture.
4. Export metadata including Portal build identity, capture start/end, active profile/scope, entry count, dropped/truncated count, session/game identifiers where available, and completeness state.
5. Export must work while the console is hidden and while Dev Tools remains open.

## Validation

1. Focused export tests for complete versus filtered output, metadata, ordering, empty capture, and truncation disclosure.
2. Portal build and applicable browser/visual validation.

## Execution Record

### Scope Completed

1. Implemented readable full-buffer text export and structured NDJSON export with ordering, capture metadata, profile/scope, retention/truncation state, and available session/game/correlation fields.
2. Kept complete-buffer export distinct from display-filtered visibility.
3. Verified export while the diagnostics window is visible and while the console is hidden; browser download behavior was manually validated.

### Files Changed

1. Portal trace normalization/export modules, Diagnostics workspace export controls, and focused export tests.

### Contract/Interface Impact

1. No shared contract change. Export remains Portal-local and suitable for later manual cross-file alignment.

### Validation Results

1. Portal test suite passes: 27 files, 217 tests.
2. Production TypeScript/Vite build succeeds.
3. Manual validation confirmed the trace file downloads successfully and contains the captured Portal records.

### Remaining Gaps

1. Trace content and verbosity will be refined as real diagnostic cases identify high-value versus repetitive events.

### Next Pass

1. F02.5 Simulator diagnostics audit — explicitly waived for F02 closure.
